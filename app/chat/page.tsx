import { Chat } from "@/components/Chat/Chat";

/**
 * Main chat page route component.
 * Serves as the primary entry point for the interactive AI Assistant chat application.
 *
 * @returns The rendered Chat container component.
 */
export default function Home() {
    return <Chat />;
}